export enum ExpenseType {
    Groceries = "Groceries",
    Clothes = "Clothes",
    Bills = "Bills"
}

export enum MonthNameLength {
    Short = 'short',
    Long = 'long',
    TwoDigits = '2-digit',
    Narrow = 'narrow',
    Numeric = 'numeric'
}